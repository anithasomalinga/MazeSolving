services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: maze
      POSTGRES_PASSWORD: maze
      POSTGRES_DB: mazedb
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: